: IMMEDIATE CODE CACHE[REG['LAST_NAME']][1] = True; REG['IP'] = RSTACK.pop(); END-CODE
: RET CODE RSTACK.pop(); REG['IP'] = RSTACK.pop() END-CODE IMMEDIATE
: ; CODE CACHE[REG['LAST_NAME']] = [REG['LAST_WORD'], False]; REG['COMPILING'] = False; CODES.append(4); REG['IP'] = RSTACK.pop(); END-CODE IMMEDIATE
: \ CODE inter_nextstring('\n') END-CODE
: # \ ; IMMEDIATE
\ now we have comment
